{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build and run",
      "type": "shell",
      "command": "cd build && make",   // change a.out if your program is named something else
    }
  ]
}
